services:
  - type: web
    name: syncstate-api
    env: node
    plan: free
    root: .
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    # Explicitly runs compiled JavaScript from dist/ folder
