services:
  - type: web
    name: syncstate-api
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    # Runs: node dist/index.js (compiled JavaScript, not TypeScript)
